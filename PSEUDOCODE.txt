START
   declare
	double carPrice, downPay, principal, monthlyRepay, totalLoan=0, totalprincipal=0, totalInt=0, balance
	int    loanPeriod, intRate
	
   input carPrice, downPay, loanPeriod, intRate
	if carPrice < 30000 then
		require user to input again
	otherwise proceed
	
	if downPay < 0
		require user to input again 
	otherwise proceed

	if loanPeriod < 5 OR loanPeriod > 9
		require user to input again
	otherwise proceed

	if intRate < 3 OR intRate > 7
		require user to input again
	otherwise proceed
	
   Calculate
	totalInt = (carPrice - downPay) * intRate/100
	principal = (carPrice- downPay) / loanPeriod + totalInt

	monthlyRepay = principal / 12	
	print monthlyRepay

	totalLoan = (carPrice - downPay) + (totalInt * loanPeriod)
	balance = totalLoan - totalprincipal;

	print i, totalprincipal, totalInt, balance






		
		
		
